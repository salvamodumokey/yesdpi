# Copy to .env.local for local overrides, or set in your hosting provider's
# project settings (e.g. Vercel → Settings → Environment Variables).
#
# Drives every absolute URL the app generates (canonical links, sitemap,
# robots.txt, Open Graph/Twitter metadata, structured data). Falls back to
# the live Vercel deployment URL if unset — see lib/config.ts.
NEXT_PUBLIC_SITE_URL=https://yesdpi.vercel.app
